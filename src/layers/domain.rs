// Domain layer - Pure business logic
// This layer contains only business logic, no dependencies on external frameworks

pub struct DomainLayer;

impl DomainLayer {
    pub fn describe() -> &'static str {
        "Domain layer - Pure business logic and rules"
    }
}
